// Si alguna vez algo no se resetea hacer ctrl + shift + p y escribir "reload window"
import { createApp } from 'vue';
import './styles.css';
import App from './App.vue';

createApp(App).mount('#app');
